# 書房 × MarginNote 4 对齐验收标准（索引）

本目录记录「向 MarginNote 4 靠拢」改造的内部验收标准与每次验证结果。

- `v1/`：第一版验收标准（2026-08-30 建立）。衡量范围：复习/FSRS、挖空回忆、卡片标签与卡片盒、脑图焦点/大纲、学习集分组、开放 API 扩展。
- `runs/`：每次运行验证的时间戳记录（命令、退出码、产出值），全部保留，不仅保留通过的检查点。

## 版本记录

| 版本 | 创建时间   | 衡量内容                                                                                           | 与上一版差异 |
| ---- | ---------- | -------------------------------------------------------------------------------------------------- | ------------ |
| v1   | 2026-08-30 | MN4 核心闭环：摘录成卡 → 脑图/卡片盒组织 → 闪卡复习（间隔重复 + 挖空）→ 回源定位；外加学习集与 API | 首版         |

## 2026-08-30 验收记录

- v1 全量通过：12/12 静态项 PASS；Playwright E2E 覆盖 A1–E1 全部交互项；API F1–F3 curl 实测通过。详见 runs/20260830-1225-final-e2e.md。
